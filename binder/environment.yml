name: hsf-india
channels:
  - defaults
  #- conda-forge
dependencies:
  - python
  - pip
  - cmake 
  - openssh 
  - singularity
  - ncurses
  - ipykernel
  - pip:
      - jupyterlab-deck
      - jupyterlab-mathjax3
      - matplotlib     
      - numpy
      - requests
      - uproot
      - fsspec-xrootd
      - xrootd
      - particle
      - scipy
      - pandas
      - vector
      - bash_kernel
     
variables:
  LD_LIBRARY_PATH: "'$LD_LIBRARY_PATH:$CONDA_PREFIX/lib/'"
